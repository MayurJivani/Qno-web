{
  "name": "qno-web",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci --include=dev && npm --prefix backend ci --include=dev && npm --prefix frontend run build && npm --prefix backend run build",
    "start": "npm --prefix backend run start"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3"
  }
}
